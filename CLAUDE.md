# CLAUDE.md

Claude Code のコンテキスト消費を可視化するローカル Web アプリ。実装は `cc_tokens_web.py` 1ファイル（約1500行）。

このファイルは毎ターン読み込まれるので短く保つこと。詳細は `docs/internals.md` を必要なときだけ読む。

## 絶対に壊してはいけない制約

1. **依存ライブラリを追加しない。** Python 3.9+ 標準ライブラリのみ。フロントも CDN 禁止（社内プロキシ環境で動かすため）
2. **外部へ送信しない。** ネットワーク呼び出しはゼロ
3. **`~/.claude` に書き込まない。** 読み取り専用
4. **`127.0.0.1` 固定 + `Host` ヘッダ検証。** DNS リバインディング対策。`0.0.0.0` に変えない
5. **ログ由来の文字列は必ず `esc()` を通す。** ファイル名やプロンプトに HTML が入りうる

## 中心となる指標

| 用語 | 定義 |
|---|---|
| context | `input_tokens + cache_creation + cache_read`。そのターンが抱えていた総量 |
| 増加 (`delta`) | `context[i] - context[i-1]` |
| 持ち越し (`carry`) | `max(delta,0) * (残りターン数)`。**このツールの主役** |

`carry` が主役。「その1回の読み込みが、以降のターンで何度も再課金された総額」を表す。
単純な context 合計だと単調増加して犯人が特定できないため、この指標を置いている。

## ファイル構造（`cc_tokens_web.py`）

```
  1- 68  ヘルパ（日付・時刻・文字列）
 69-238  LogStore: JSONL の差分パース、enrich()（delta/carry を計算）
239-366  集計: summarize / hour_detail / detail
367-468  HTTPハンドラ（4エンドポイント）と main()
470-      PAGE = 埋め込み HTML/CSS/JS（1ファイルで完結）
```

エンドポイントは `/`, `/api/data`, `/api/hour`, `/api/session` の4つだけ。増やす場合は `_host_ok()` を通ること。

## UI の構造

- ビューは3つ: `view-list` / `view-timeline` / `view-detail`。`showView(id)` で切替
- ドリルは4段: 時間 → セッション → ターン → ツール詳細（`#sheet` パネル）
- テーマは4つ: `8bit` / `pop` / `light` / `dark`。CSS 変数 + JS の `THEMES` 定数

## 変更前に必ず読むこと

`docs/internals.md` に、実装中に踏んだ罠と、その回避が今のコードのどこに埋まっているかを書いてある。
特に **描画性能・パース・レイアウト・アニメーション** を触る場合は先に読む。
知らずに触ると、動くが遅い／実データでだけ壊れるコードになる。

## 動作確認

```bash
python3 cc_tokens_web.py --dir <ダミーログ> --port 8899 --no-open
```

UI を変更したら、目視ではなく **DOM を計測して**確認する（手順は `docs/internals.md`）。
実データ規模（1000セッション超）でしか出ない不具合があるため、少数データだけの確認では不十分。
